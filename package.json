{
  "name": "agent-relay",
  "private": true,
  "workspaces": ["server", "client"],
  "scripts": {
    "server": "npm run dev --workspace=server",
    "client": "npm run dev --workspace=client",
    "build": "npm run build --workspace=client",
    "kill": "node scripts/free-port.js 3017 5173"
  },
  "allowScripts": {
    "node-pty": true,
    "esbuild": true
  }
}
