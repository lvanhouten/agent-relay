## Remediation Verification: full-repo-audit — 82b61ae..3166587

**Verifies:** _docs/work/full-repo-audit/adversarial-review-full-repo-9e35ffa..82b61ae.md
**Range:** 82b61ae..3166587
**Verdict:** CLEARED

Method: independent re-read of the full diff and the full text of every touched file (`server/board/board.js`, `server/board/lib.js`, `server/board/mcp-server.js`, `server/src/auth.js`, plus all four test files), the annotated review doc, and the full test suite (72/72 pass, up from 61 at the original review's baseline). Each Resolution's narrative was treated as a claim to break, not an explanation to accept — traced independently from the code, not from the write-up.

### Close-out (original findings)

| Orig ID | Claimed | Verify verdict | Evidence |
|---------|---------|----------------|----------|
| C1 | Resolved (A) in 64e9250 | ✅ Confirmed closed | Both `net.createServer` callbacks (`board.js:139`, `board.js:336`) now route pre-auth bytes through the shared `makeHandshake` (`lib.js:111-124`), which caps the no-newline accumulator at `MAX_PREAUTH_BYTES` (4096) and returns `overflow` instead of growing unbounded — verified the control-plane path (`board.js:341-345`) takes the identical `gate.feed()`/destroy-on-overflow branch the data plane always had. Post-auth, the control plane's `cmdBuf` (a genuinely new accumulator this fix introduces) is capped at `MAX_CMD_BYTES` (1MB, `board.js:368`), closing the *other* half of C1 (an authed oversized newline-less command) that the original finding also called out. `lib.test.js`'s `makeHandshake` tests reproduce both the split-chunk accept path and the overflow path at the real production cap. |
| C2 | Resolved (B) in 88907a7 | ✅ Confirmed closed | `bringOnline` (`board.js:395-399`) generates and assigns `SECRET` in memory *before* calling `listen()`, and persists to disk only from inside the bind-success callback. Traced the actual race: Node's event loop is single-threaded, and `persistSecret`'s writes (`fs.mkdirSync`/`fs.writeFileSync`) are synchronous, executing to completion inside the same call stack as the `'listening'` event before any queued `'connection'` event can be dispatched — so the "residual sub-millisecond window" the Resolution itself flags is not just narrowed but effectively eliminated (a stronger result than claimed, not a weaker one). A process that loses the bind (`EADDRINUSE` → the existing `board.on('error', ...)` handler → `process.exit(0)`) never reaches `persist` — confirmed the callback registered via `listen(cb)` is only invoked on a successful bind, never on the `'error'` path. `board.test.js`'s two `bringOnline` tests pin exactly this ordering with a fake bind. |
| W2 | Resolved (B, re-framed) in 64e9250 | ✅ Confirmed closed | The actual root cause of C1 — two independently hand-rolled accumulate/compare copies — is gone; both `board.js` call sites now call the single `makeHandshake` in `lib.js`, so the cap can't diverge from the compare again (mechanically verified, not just asserted). The `secretEqual`/`safeEqual` half was reframed rather than merged; confirmed the reciprocal cross-reference comment actually landed at `server/src/auth.js:18-23` ("Deliberately a twin of board/lib.js's secretEqual... Keep the two in sync by hand"), matching the existing "mirrors src/auth.js" comment at `lib.js:71`. The package-boundary argument (board kernel runs standalone via `sb`/`mcp-server` with no dependency on `server/src`) holds — confirmed via `require` statements: nothing under `server/board/` imports from `server/src/`. This is a legitimate B-reframe, not a dodge: the concern that actually caused C1 (the handshake) is centralized; the compare-twin's "nothing keeps them in sync" complaint is now at least surfaced to a future reader. |
| W3 | Resolved (A, client-side minimum) in b186f25 | ✅ Confirmed closed (see new finding N-new1 for a related diagnostic gap introduced alongside it) | `readClosedBeforeOutput(text, pipeClosed)` (`mcp-server.js:114-116`) is exactly `pipeClosed && text.length === 0`, and `finish()` in `readOutput` (`mcp-server.js:148-153`) now rejects with `EREADCLOSED` before advancing the cursor when that's true. Traced all three cited invariants against the actual code: a genuinely-quiet-but-healthy line's `finish()` always fires via the `quiet`/`hardStop` timers with `pipeClosed=false` (the data-plane socket is never closed by the board while a client is attached and idle) — never trips the new branch; a normal line exit always writes the non-empty farewell sentinel to an already-authed client before closing (`board.js:174`, `notifyClientsClosed`) — also never trips it. The only path that reaches `pipeClosed && text===''` is a socket destroyed pre-auth or mid-handshake, exactly the failure class the finding named. `mcp-server.test.js`'s three `readClosedBeforeOutput` cases pin all three branches. The core ask — "an auth failure surfaces as an error rather than a false-quiet read" — is met. |
| W1 | Deferred (D) — issue doc | — Not addressed (by design) | Confirmed genuinely deferred, not silently dropped: `_docs/issues/2026-07-01-secret-file-acl-verification.md` exists, correctly scopes the gap (Windows `mode` bits are inert; the real boundary is the inherited profile ACL, never live-verified the way the pipe DACL was), and states an honest reason for not fixing now (the Windows ACL-verification arm needs a multi-user test rig this worktree doesn't have; shipping only the POSIX `chmod` arm would leave the primary Windows gap open while implying more coverage than delivered). Matches the doc's own scope note that this run was C1/C2/W1/W2/W3 only. |

### New findings (introduced by the remediation)

**W-new1. The new `EREADCLOSED` error's carefully-crafted diagnostic message is discarded before it ever reaches the caller, undermining W3's stated purpose** — `server/board/mcp-server.js:287` (pre-existing catch, newly relevant) vs `server/board/mcp-server.js:149` (the message it discards) · confidence 70

`readOutput`'s `finish()` now throws `new Error('read failed: the board closed the connection before any output (line missing, or the access secret was rejected)')` with `err.code = 'EREADCLOSED'` (`mcp-server.js:149-151`) — a message written specifically to give an operator the actionable "the access secret was rejected" signal W3 exists to surface. But `switchboard_read_output`'s handler (a pre-existing catch block this diff never touched) formats the surfaced text as `` `no such line, or it has ended (${e.code || e.message})` `` (`mcp-server.js:287`). Since `e.code` is truthy (`'EREADCLOSED'`), the `||` short-circuits and the descriptive `e.message` is never used — the agent only ever sees "no such line, or it has ended (EREADCLOSED)", which reads almost identically to the pre-existing generic "line doesn't exist" case this new error was supposed to be distinguished from. W3's literal contract (surface an error, not a false-quiet empty string) is met — this is why the close-out above is Confirmed closed, not Not closed — but the fix's own stated value ("so an auth failure surfaces as an error rather than a false-quiet read" with an explanation of *why*) is largely defeated at the one place a human or agent actually reads it. This is a pre-existing line the remediation's own new error path makes newly relevant (the fair-game interaction-finding carve-out), not a pre-existing-and-irrelevant issue.

**Fix:** in the catch block, prefer `e.message` when it's more than a bare code, e.g. `` e.code === 'EREADCLOSED' ? e.message : `no such line, or it has ended (${e.code || e.message})` ``.

**N-new1. `MAX_CMD_BYTES`, the new post-auth control-plane cap, has no test exercising it** — `server/board/board.js:368` · confidence 45

C1's closure check (both the annotated doc's Resolution and `lib.test.js`) covers only the pre-auth `makeHandshake` cap. The post-auth `cmdBuf.length > MAX_CMD_BYTES` check in the control-plane's `net.createServer` callback is new code (there was no post-auth accumulator or cap of any kind before this fix) with no unit or integration test driving an authed connection past 1MB of newline-less bytes to confirm it actually destroys the socket rather than, say, an off-by-one letting it accumulate one chunk further before tripping. The logic is simple and was read and traced by hand above, but per the Saboteur's untested-failure-mode rule, new logic defending against the same daemon-crash shape as C1 shipping with zero coverage is worth naming, even though the code itself looks correct on inspection.

**Fix:** a `board.test.js` case that feeds a fake authed socket a large newline-less chunk (or several) past `MAX_CMD_BYTES` and asserts the socket is destroyed — mirroring the `makeHandshake` overflow tests' shape.

### Summary

All four in-scope findings (C1, C2, W2, W3) are genuinely closed — the fixes were re-derived from the code independently, not accepted from the Resolutions' narratives, and in C2's case the actual guarantee is stronger than the Resolution itself claims (the "residual window" it flags doesn't survive tracing Node's single-threaded event loop against a synchronous `persistSecret`). W1 remains a legitimate, well-scoped deferral. The sweep found no regressions and no new criticals: W-new1 is a real but non-blocking diagnostic-quality gap in a pre-existing line the fix made newly relevant, and N-new1 is a coverage gap on otherwise-correct new code. Safe to merge the worktree; W-new1 is cheap enough to fold in first if convenient, but does not gate the merge.

## Priority ranking

| ID | Severity | Conf | Finding | Status |
|----|----------|------|---------|--------|
| W-new1 | WARNING | 70 | `EREADCLOSED`'s descriptive message is discarded by the pre-existing generic catch formatting | (open) |
| N-new1 | NOTE | 45 | New post-auth `MAX_CMD_BYTES` cap has no covering test | (open) |
